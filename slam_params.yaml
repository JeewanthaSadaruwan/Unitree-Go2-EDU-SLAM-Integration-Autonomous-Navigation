slam_toolbox:
  ros__parameters:
    use_sim_time: false
    mode: mapping

    scan_topic: /scan

    map_frame: map
    odom_frame: odom
    base_frame: base_link

    transform_timeout: 0.2
    tf_buffer_duration: 10.0
    resolution: 0.05
    max_laser_range: 30.0
